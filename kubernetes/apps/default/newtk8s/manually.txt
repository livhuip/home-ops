helm repo add fossorial https://charts.fossorial.io
helm repo update fossorial
helm install newt fossorial/newt \
    --create-namespace \
    --set newtInstances[0].name="main-tunnel" \
    --set-string newtInstances[0].auth.keys.endpointKey="https://pangolin.hayani.xyz" \
    --set-string newtInstances[0].auth.keys.idKey="o1dmghwxl799mca" \
    --set-string newtInstances[0].auth.keys.secretKey="256t2np06yomo9h3vxrm41djk8gdtlk1op08ai2ptlco6sly"